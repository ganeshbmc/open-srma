// Copy this file to `.env` and customize values as needed.

# Security
SECRET_KEY=change-this-in-production

# Flask
FLASK_APP=run.py
# Enable debug locally (do not use in production)
# FLASK_ENV=development
# FLASK_DEBUG=1

# Database: choose ONE
# If unset, the app uses SQLite at `instance/srma.db` automatically.
# For Postgres (recommended for production), uncomment and set:
# DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DBNAME

# Networking (optional; platforms usually set PORT)
# HOST=0.0.0.0
# PORT=8000

